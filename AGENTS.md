# AGENTS.md - datalens

This repository follows the parent `/code/helixbox/AGENTS.md` constitution.

## Project Notes

- Keep initialization and future changes minimal and code-focused.
- Put durable documentation under `docs/` using `docs/policy.md`.
- Do not commit secrets, binary artifacts, archives, or generated local outputs.
